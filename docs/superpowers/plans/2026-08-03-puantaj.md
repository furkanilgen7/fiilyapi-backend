# Puantaj — timesheet (uygulama planı)

Spec: `../specs/2026-08-03-puantaj-design.md` · Ön şart: spec §7 sorularının kullanıcı cevabı.
Dal: `feat/puantaj` (GÜNCEL main'den; **TB2 merge'liyse onu içerir — `alembic heads` ile tek head
doğrula, ebeveyn açık revizyon id**). Her task = tek subagent + commit · TDD · DB komutları HEP
override'lı (WORKFLOW §4). **ARA REVIEW YOK** — tek kalite kapısı T5 FINAL REVIEW (WORKFLOW §2 kuralı).

## T1 — Migration + modeller
- `personnel` + `timesheet_entries` + `timesheet_code` enum (worker_source enum'u YENİDEN kullanılır,
  yenisi açılmaz); CHECK + UQ'lar (kişi-gün tekliği). İzin migration'ı GEREKMEZ (`timesheet` +
  `personnel` seed'de hazır) — `test_seed_matrix` yeşilliği kanıtlanır.
- upgrade→downgrade→upgrade turu; downgrade'de `DROP TYPE` (yalnız yeni enum).

## T2 — personnel modülü (TDD)
- Router/service/repository/schemas: liste (arama+filtreler) · POST · GET/PATCH · silme YOK
  (`is_active` pasifleştirme). İzin `personnel` (İK full, şef view). Audit. Proje süzgeci YOK
  (şirket-geneli İK varlığı) — bu bilinçli, teste yazılır.

## T3 — timesheet matris uçları (TDD)
- `GET .../timesheet` matris + türev toplamlar (kişi adam-gün, günlük sayılar, `+`/`G` işaretleri,
  FM saat toplamı S2'ye göre) + bölüm filtresi.
- `PUT .../timesheet` toplu DEĞİŞTİRME (dönem+şantiye kapsamı; gönderilmeyen hücre silinir) +
  FOR UPDATE kilit + kişi-gün UQ ihlalinde (başka şantiyede kaydı olan personel) anlaşılır 409 ·
  audit tek dönem-özeti olayı. İzin `timesheet` (şef full, saha müh. view — view PUT'a giremez, test).
- IDOR: `visible_projects` süzgeci her uçta.

## T4 — Export + yer tutucu bağlama (TDD)
- `GET .../timesheet/export.xlsx` (boq/audit export deseni; Content-Type xlsx).
- `sites/projects` servislerindeki `_TIMESHEET` worker_count yer tutucuları gerçek sayıya bağlanır
  (dönem içinde kaydı olan distinct personel); etkilenen KPI testleri güncellenir.

## T5 — Kapanış + FINAL REVIEW (Opus)
- Tüm paket + ruff (tüm repo) + `alembic check` temiz + migration turu.
- openapi üret → **DEVİR KURALI (WORKFLOW §4):** frontend checkout main'de+temiz mi? Değilse KOPYALAMA,
  rapor; temizse kopyala + not (`gen:api` frontend main'inde ayrı commit; BFF kökleri `personnel` +
  `timesheet` ekran diliminin işi).
- Review odağı: IDOR · kişi-gün tekliği yarışları · PUT değiştirme kapsam sınırı (başka ayın/şantiyenin
  hücresine dokunulmadığı KANIT) · izin ayrımları (personnel vs timesheet) · kalıcı karar taraması
  (İK sızıntısı: belge/izin/SGK/bordro alanı açılmamış olmalı).
- `ARCHITECTURE-BACKEND.md` + `ROADMAP-BACKEND.md` güncellenir, commit. Push/PR/merge/deploy kullanıcıda.
